STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
REDIS_URL=redis://localhost:6379
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=
DB_NAME=medusa-v2

# Stripe (for checkout). Get keys from https://dashboard.stripe.com/apikeys (use Test mode for testing).
STRIPE_API_KEY=sk_test_
# Optional: for production webhooks. See https://docs.medusajs.com/plugins/payment/stripe
# STRIPE_WEBHOOK_SECRET=whsec_

# Optional: when set, every placed order (checkout, recharge, SIMs, accessories) is POSTed here (testing / X service).
# Body: JSON with order_id, display_id, email, created_at, items[], shipping_address.
# To use the built-in X service in the Next.js frontend: ORDER_WEBHOOK_URL=http://localhost:8000/api/order-webhook
# ORDER_WEBHOOK_URL=https://your-x-service.example.com/orders

# Email (Gmail) – free, send from your Gmail (e.g. manavpandya7603@gmail.com).
# Create App Password at https://myaccount.google.com/apppasswords (need 2-Step Verification first).
# Without these, emails are logged only, not sent.
# GMAIL_USER=yourname@gmail.com
# GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx
